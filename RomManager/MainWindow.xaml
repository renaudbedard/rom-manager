<Window x:Class="RomManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RomManager"
        Title="ROM Manager" Height="720" Width="1280">

    <Window.DataContext>
        <local:GameLibrary />
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type local:Game}">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="6">
                <!-- Drop Shadow -->
                <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CornerRadius="4"
                        Background="#44000000">
                    <Border.RenderTransform>
                        <TranslateTransform X="5" Y="5" />
                    </Border.RenderTransform>
                    <Border.BitmapEffect>
                        <BlurBitmapEffect Radius="8" />
                    </Border.BitmapEffect>
                </Border>
                <!-- Image Template -->
                <Border Padding="4" Background="White" BorderBrush="#22000000" BorderThickness="1">
                    <Image Source="{Binding FrontCover}" 
                           Height="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Path=DataContext.ThumbnailSize}" />
                </Border>
            </Grid>
        </DataTemplate>

        <ControlTemplate x:Key="NoScroll">
            <ItemsPresenter></ItemsPresenter>
        </ControlTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="150*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Label Content="Current folder :" VerticalAlignment="Center" FontWeight="Bold" />
            <Label Content="{Binding FolderPath, TargetNullValue='(none)'}" FontFamily="Consolas" VerticalAlignment="Center" Height="26" Padding="0,7,0,0" Margin="0,7,5,7" />
            <Button Name="BrowseButton" Content="Browse" Margin="5,7" Padding="10,0,10,0" Click="BrowseButton_Click" />
            <Button Name="AnalyzeStopButton" IsEnabled="{Binding HasFolderPath}" Content="{Binding AnalyzeStopCaption}" Margin="5,7" Padding="10,0,10,0" Click="AnalyzeButton_Click" />
            <Slider x:Name="ThumbnailSizeSlider" Width="333" VerticalAlignment="Center" Margin="5,11,0,11" Maximum="512" Minimum="8" SmallChange="1" Value="{Binding ThumbnailSize}" LargeChange="16" TickFrequency="16" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="635*"/>
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="96*"/>
                    <ColumnDefinition Width="537*"/>
                </Grid.ColumnDefinitions>
                <ScrollViewer Grid.Column="0">
                    <ListBox Template="{StaticResource NoScroll}"  ItemsSource="{Binding Games}" SelectedItem="{Binding CurrentGame}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </ScrollViewer>

                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />

                <ScrollViewer Grid.Column="2" Grid.ColumnSpan="2">
                    <StackPanel DataContext="{Binding CurrentGame}">
                        <Label Content="{Binding Title}" FontWeight="Bold" FontSize="16"/>

                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding FrontCover}" Height="512" Margin="5"/>
                            <Image Source="{Binding BackCover}" Height="512"/>
                        </StackPanel>

                        <Label Content="Description" FontStyle="Italic"/>
                        <Label>
                            <TextBlock TextWrapping="WrapWithOverflow" Text="{Binding Description}" />
                        </Label>

                        <!--
                        <Label Content="Rating" FontStyle="Italic"/>
                        <Label>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} ({1} ratings)">
                                        <Binding Path="Rating" />
                                        <Binding Path="RatingCount" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Label>
                        -->
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
