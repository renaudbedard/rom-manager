<Window x:Class="RomManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RomManager"
        mc:Ignorable="d"
        Title="ROM Manager" Height="350" Width="525" Loaded="Window_Loaded">
    <Window.Resources>
        <DataTemplate DataType="{x:Type local:Game}">
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="6">
                <!-- Drop Shadow -->
                <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CornerRadius="4"
                        Background="#44000000">
                    <Border.RenderTransform>
                        <TranslateTransform X="5" Y="5" />
                    </Border.RenderTransform>
                    <Border.BitmapEffect>
                        <BlurBitmapEffect Radius="8" />
                    </Border.BitmapEffect>
                </Border>
                <!-- Image Template -->
                <Border Padding="4" Background="White" BorderBrush="#22000000" BorderThickness="1">
                    <Image Source="{Binding Cover.BitmapImage}" />
                </Border>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid DataContext="{DynamicResource Games}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260*"/>
            <ColumnDefinition Width="259*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="listBox" ItemsSource="{Binding}"/>
        <StackPanel Grid.Column="1">
            <Label x:Name="NameHeader" Content="Name"/>
            <Label x:Name="NameLabel" Content=""/>
            <Label x:Name="CoverArtHeader" Content="Cover Art"/>
            <Image x:Name="CoverArtImage" Height="100"/>
        </StackPanel>

    </Grid>
</Window>
